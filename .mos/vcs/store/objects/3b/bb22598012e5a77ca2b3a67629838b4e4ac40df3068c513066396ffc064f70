blob
contract "CON-2026-187" {
  title = "Rich query: return all artifact fields and support filter predicates"
  status = "complete"
  created_at = 2026-03-05T10:16:08Z
  updated_at = 2026-03-05T10:16:08Z
  goal = "mos query --format json returns full artifact content (title, goal, justifies, depends_on, priority, slug, all fields) instead of the thin {ID, Kind, Status, Sprint, Path} envelope. Support filter flags: --justifies, --sprint, --depends-on, --priority, --unlinked (no sprint). Eliminates the N+1 subprocess pattern where agents must call mos get per field per artifact."
  justifies = "NEED-2026-002"
  kind = "feat"
  priority = "high"
  slug = "rich-query-filters"
  sprint = "SPR-2026-015"
}

