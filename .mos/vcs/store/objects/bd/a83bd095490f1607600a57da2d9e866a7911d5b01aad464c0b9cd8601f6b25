blob
batch "BAT-2026-006" {
  status = "promoted"
  goal = "CLI quality-of-life improvements that prioritize AI agent ergonomics. The CLI is the API — agents use the same commands humans do. If the CLI is hostile to agents (no help, no JSON, no bulk operations), the Console First principle fails. These contracts make mos agent-native without sacrificing human usability."
  title = "Agentic Workflow QoL"
  priority = "high"
  slug = "agentic-workflow-qol"
  sprint = "SPR-2026-005"
}

