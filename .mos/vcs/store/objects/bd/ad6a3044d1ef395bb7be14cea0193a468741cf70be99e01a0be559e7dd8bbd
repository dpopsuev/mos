blob
contract "CON-2026-226" {
  title = "mos set kind should re-prefix or reject mismatched ID prefix"
  status = "complete"
  created_at = 2026-03-05T21:44:06Z
  updated_at = 2026-03-05T21:44:06Z
  goal = "When mos set changes the kind field (e.g. BUG-2026-023 kind=feature), the ID prefix becomes inconsistent with the kind. Either re-prefix the artifact (rename directory, update references) or reject the change with an error guiding the user to use mos reclassify instead."
  kind = "bug"
  slug = "kind-prefix-mismatch"
}

