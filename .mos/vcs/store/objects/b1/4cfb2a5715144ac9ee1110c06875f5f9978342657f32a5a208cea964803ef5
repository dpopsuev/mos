blob
specification "SPEC-2026-011" {
  title = "Hybrid Coexistence Model"
  status = "candidate"
  enforcement = "disabled"
  name = "Hybrid Coexistence Model"
  slug = "hybrid-coexistence"
  description = "Defines how .mos and .git coexist in a repository. In hybrid mode, .mos rides on top of .git — governance artifacts are Git-tracked files with .mos providing resolution control, drift tracking, and enforcement. Covers: Git hook integration (pre-commit, post-merge), merge conflict semantics for .mos state, .gitignore vs .mosignore boundaries, and the migration path from hybrid to pure .mos. A pure .mos repository provides version control natively without .git. Manifesto Section 5."

  feature "Git hook integration" {
  }

  feature "Merge conflict semantics" {
  }

  feature "Pure .mos mode" {
  }
  justifies = "NEED-2026-001"
  state = "desired"
  group = "federation"
}

