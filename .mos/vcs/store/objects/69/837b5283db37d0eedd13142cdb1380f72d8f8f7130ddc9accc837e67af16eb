blob
specification "SPEC-2026-010" {
  title = "Gateway Adapter Interface"
  status = "candidate"
  enforcement = "disabled"
  description = "Defines the interface contract for bidirectional adapters (Gateways) that synchronize external systems — issue trackers, CI pipelines, infrastructure dashboards, wikis — with the .mos constitutional state. Constitution is the hub; external systems are the spokes. Each Gateway must provide: inbound sync (external to .mos), outbound sync (.mos to external), conflict resolution, provenance tracking, and graceful degradation when the external system is unreachable. Manifesto Sections 4 and 8."
  name = "Gateway Adapter Interface"
  slug = "gateway-adapter-interface"

  feature "Bidirectional sync" {
  }

  feature "Graceful degradation" {
  }

  feature "Conflict resolution" {
  }
  justifies = "NEED-2026-001"
  state = "desired"
  group = "federation"
}

