blob
contract "CON-2026-168" {
  title = "Replace magic strings with registry-derived constants"
  status = "complete"
  created_at = 2026-03-04T18:44:47Z
  updated_at = 2026-03-04T18:44:47Z
  goal = "Eliminate scattered string literals like \"contracts\", \"rules\", \"specifications\" from governance/ and cmd/mos/ packages. Define constants (or derive from registry type definitions) for all artifact kind names, directory names, and status values. Target: zero raw kind-name string literals in production code outside of registry definition."
  kind = "feature"

  acceptance {

    criterion "zero-raw-literals" {
      description = "Zero raw kind-name string literals in governance/*.go and cmd/mos/**/*.go production code outside registry definitions"
    }

    criterion "constants-defined" {
      description = "Constants or registry-derived values defined for all artifact kind names, directory names, and status values"
    }

    criterion "build-clean" {
      description = "go build ./... and go test ./... pass with zero errors"
    }
  }
  sprint = "SPR-2026-011"
}

