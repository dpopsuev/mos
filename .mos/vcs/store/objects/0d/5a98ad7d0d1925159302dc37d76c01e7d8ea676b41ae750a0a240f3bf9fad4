blob
specification "SPEC-2026-012" {
  title = "Harness Verification Quadrants"
  status = "active"
  enforcement = "disabled"
  name = "Harness Verification Quadrants"
  slug = "harness-verification-quadrants"
  description = "Formalizes the two orthogonal axes of Harness verification. Axis 1: Gate (mechanical, deterministic, binary pass/fail) vs Advisory (interpretive, judgment-based, shaped). Axis 2: Blue Team (benevolent, positive space — proving invariants hold) vs Red Team (adversarial, negative space — probing that invariants resist violation). The four quadrants: Gate+Blue (Gherkin scenarios pass), Gate+Red (adversarial fuzz/probe finds crash), Advisory+Blue (spec appears satisfied), Advisory+Red (adversarial review finds spirit violated despite letter met). PoC implements Gate+Blue; this spec defines all four. Manifesto Section 9."

  feature "Gate plus Blue quadrant" {
  }

  feature "Gate plus Red quadrant" {
  }

  feature "Advisory plus Blue quadrant" {
  }

  feature "Advisory plus Red quadrant" {
  }
  justifies = "NEED-2026-001"

  spec {
    include "moslib/harness"
  }
  state = "current"
  group = "harness"
}

