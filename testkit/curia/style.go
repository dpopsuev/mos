package main

// Styles are now in the Styles struct (theme.go), built dynamically from the
// active Theme via BuildStyles(). This file is kept as the package's style
// documentation anchor.
//
// The model carries *Styles and passes it to render functions.
// See theme.go for Palette, Theme, and BuildStyles().
